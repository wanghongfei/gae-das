spring:
  application:
    name: GAE-DAS
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gae-das
    username: root


das:
  mysql:
    host: 127.0.0.1
    port: 3306
    username: root
    password:
  store:
    file:
      enable: true
      path: index/incr.idx

logging:
  level:
    org.fh.gae.das: debug
  pattern:
    console: "[%d{HH:mm:ss}] [%thread] %-5level %logger{36} - %msg%n"

